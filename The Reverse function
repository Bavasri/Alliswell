#Alliswell
Developed by S.Bavasri

OUTPUT:
(https://raw.githubusercontent.com/Bavasri/Alliswell/main/reverse.PNG)

#include <stdio.h>
#include <string.h>
int main()
{
   char s[100];

   printf("Enter a string to reverse: ");
   gets(s);

   strrev(s);

   printf("\nReverse of the string:  %s\n", s);

   return 0;
}
